# Moviepedia

### Installation
1. Setup environment

    ```
    python -m venv env
    ```

2. Install requirements
    ```
    pip install -r requirements.txt
    ```

3. Run app
    ```
    flask run
    ```
